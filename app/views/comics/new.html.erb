<div class="container">
    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            <div class="d-flex align-items-center">
              <h2>新しいマンガを追加する</h2>
            <!--作品一覧に戻るボタンをここに追加-->
            </div>
            <%= form_for @comic do |f| %>
                <%= f.label :image, '作品の画像' %>
                    <div class="form-group">
                    <%= f.attachment_field :image %>
                     <small id="title-tip" class="form-text text-muted">
                        作品のイメージ画像があればアップロードしてください。
                    </small>
                    </div>
                    <div class="form-group">
                        <%= f.label :title, '作品タイトル' %>
                        <%= f.text_field :title, class: 'form-control' %>
                        <small id="title-tip" class="form-text text-muted">
                            タイトルを入力してください。
                        </small>
                    </div>
                    <div class="form-group">
                        <%= f.label :explanation, '作品のストーリー紹介' %>
                        <%= f.text_area :explanation, class: 'form-control', rows:10 %>
                        <small id=content-tip" class="form-text text-muted">
                            作品のストーリ内容・あらすじを入力してください。 
                        </small>
                    </div>
                    <%= f.submit '追加', class: 'btn btn-info btn-block' %>
                    <small id="submit-tip" class="form-text text-muted">
                    </small>
            <% end %>
        </div>
    </div>
</div>