<div class="container">
    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            <div class="d-flex align-items-center">
              <h2>作品情報を編集する</h2>
            </div>
            <%= form_for @comic,url: admins_comic_path ,method: :patch do |f| %>
                <%= f.label :image, '作品の画像' %>
                    <div class="form-group">
                    <%= f.attachment_field :image %>
                     <small id="title-tip" class="form-text text-muted">
                        作品のイメージ画像を変更する場合は新たにアップロードしてください。
                    </small>
                    </div>
                    <div class="form-group">
                        <%= f.label :title, '作品タイトル' %>
                        <%= f.text_field :title, class: 'form-control' %>
                        <small id="title-tip" class="form-text text-muted">
                            タイトルを入力してください。
                        </small>
                    </div>
                    <div class="form-group">
                        <%= f.label :author, '作者名' %>
                        <%= f.text_field :author, class: 'form-control' %>
                        <small id="title-tip" class="form-text text-muted">
                            投稿する作品の作者名を入力してください。
                        </small>
                    </div>
                    <div class="form-group">
                        <%= f.label :explanation, '作品のストーリー紹介' %>
                        <%= f.text_area :explanation, class: 'form-control', rows:10 %>
                        <small id="content-tip" class="form-text text-muted">
                            作品のストーリ内容・あらすじを入力してください。
                        </small>
                    </div>
                    <div class="form-group">
                        <%= f.label :tag_name, '作品のタグ登録' %>
                        <%= f.text_field :tag_name, value: @tag_list, class: 'form-control' %>
                        <small id="content-tip" class="form-text text-muted">
                            作品の特徴を表すタグを登録してください。<br>
                            「 , 」を入力することで複数のタグを付けることができます。<br>
                            例:バトル,ギャグ,スポーツ,ホラー
                        </small>
                    </div>
                    <p><%= f.submit '変更を確定', class: 'btn btn-info btn-block' %>
                    <small id="submit-tip" class="form-text text-muted">
                    </small></p>
                    <p><%= link_to "作品情報とレビュー一覧に戻る", admins_comic_path(@comic), class: 'btn btn-info btn-block' %></p>
            <% end %>
            <br>
        </div>
    </div>
</div>